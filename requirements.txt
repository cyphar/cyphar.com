flask
flask-flatpages
pygments
bleach
smartypants
gunicorn
feedwerk

# We have to use git here because there aren't any proper PyPi builds.
git+https://github.com/jambonrose/markdown_superscript_extension.git
git+https://github.com/jambonrose/markdown_subscript_extension.git
# We need to install setuptools because markdown requires pkg_resources but on
# Python>=3.13 this breaks.
setuptools<81
